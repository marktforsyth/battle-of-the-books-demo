namespace TextData {
  export enum Size {
    Small = "1rem",
    Medium = "1.5rem",
    Large = "2rem",
  }

  export enum Alignment {
    Left = "left",
    Center = "center",
    Right = "right",
  }

  export enum Color {
    Main = "#bdc1c6",
    Quiet = "#858a8e",
  }
}

export default TextData;
